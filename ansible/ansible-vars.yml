---
# OpenShift UPI GCP Ansible Variables
# Copy this file and customize for your environment

# GCP Configuration
gcp_project_id: "openenv-h2zff"

# OpenShift Configuration  
cluster_name: "ocp"
domain_name: "ocp.h2zff.gcp.redhatworkshops.io"

# Infrastructure Configuration
bastion_zone: "us-central1-a"
bastion_user: "ubuntu"

# DNS Configuration
dns_zone: "ocp-zone"

# File Paths
ssh_key_path: "../keys/id_rsa"
kubeconfig_path: "../clusterconfig/auth/kubeconfig"

# Zone Configuration for multi-zone deployment
control_plane_zones:
  - "us-central1-a"
  - "us-central1-b" 
  - "us-central1-c"

worker_zones:
  - "us-central1-a"
  - "us-central1-b"
  - "us-central1-c"

# RHCOS Configuration
rhcos_version: "4.19.10"
rhcos_image_name: "rhcos-4-19-10"

# GCP Operator Service Account Key (generated by Terraform)
operator_sa_key_path: "../creds/operator-sa-key.json"

# Timeouts (seconds)
api_ready_timeout: 300
csr_approval_retries: 5


